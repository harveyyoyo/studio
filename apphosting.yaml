# Firebase App Hosting configuration for Next.js SSR
# Docs: https://firebase.google.com/docs/app-hosting

runConfig:
  minInstances: 0
  maxInstances: 2
  cpu: 1
  memoryMiB: 512
  concurrency: 80

env:
  - variable: DEV_PASSCODE
    secret: DEV_PASSCODE
